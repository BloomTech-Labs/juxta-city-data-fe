import React from 'react';
import styled from 'styled-components';

const Icon = styled.p`
  color: white;
  margin: 0 0 0 7px;
`;
const HeartIcon = () => {
  return <Icon>&hearts;</Icon>;
};

export default HeartIcon;
